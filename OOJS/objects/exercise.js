import { select, listen, sleep} from './utils.js';

// 'movies' is  now an array of movies

// Function: searchMovies
// Filter to compare the user input with the moviiies title
// return the movies found

// Function listMovies(inout)
// const ocurrences = searchMovies(inout);
// if ocurrences.length > 0 -> list the movies
// interact through ocurrences and set movie titles to a <li>s
// <li> -> <ul>
// else -> movie not found